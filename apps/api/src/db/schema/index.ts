// Domain schemas land here as ERP modules are built — one file per domain
// area (e.g. `orders.ts`, `inventory.ts`, `production.ts`), re-exported from
// this index. This is the single source of truth for the database schema;
// no other package or app defines tables. See docs/decisions/003-database-ownership.md.
export {};
