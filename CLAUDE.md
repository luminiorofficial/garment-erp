This is a pnpm + Turborepo monorepo. See [docs/architecture.md](docs/architecture.md) and [docs/decisions](docs/decisions/) for how it's structured and why.

Working in `apps/web`? Next.js in this repo has its own agent docs at `apps/web/AGENTS.md` — read those before writing frontend code, since `next dev` regenerates them and they cover breaking changes specific to the installed Next.js version.
